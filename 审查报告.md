─── apps/web/src/admin/admin.css:0-0 ───
[maintainability · medium] `.fa-empty` 使用了硬编码的 `font-size: 0.7rem`，未使用设计系统定义的
`--fa-text-xs`（0.6875rem）变量。同样，`.fa-placeholder p` 使用了 `font-size: 0.75rem` 而非
`--fa-text-sm`。这些硬编码值会导致与设计系统脱节，后续修改主题或调整字号时需要额外维护。

  .fa-empty {
-   font-size: 0.7rem;
+   font-size: var(--fa-text-xs);
  }


─── apps/web/src/admin/admin.css:3574-3590 ───
[maintainability · medium] `.fa-settings-quick` 选择器在文件中出现了两次定义。第一次定义包含 `display: flex; align-items:
center; flex-wrap: wrap; gap: var(--fa-space-2);` 和子元素规则（如 `> button, a` 等），第二次定义添加了 `border-radius:
var(--fa-radius-lg);` 并重新定义了 `> button` 的样式（缺少 `text-decoration: none` 和 `transition`
属性）。这种分散定义容易造成混淆，后续修改时可能遗漏其中一个导致样式不一致。

+ /* 建议合并为一个规则块 */
  .fa-settings-quick {
-   border-radius: var(--fa-radius-lg);
- }
- .fa-settings-quick > button {
-   display: inline-flex;
-   min-height: var(--fa-control-h-sm);
+   display: flex;
    align-items: center;
-   gap: var(--fa-space-1);
-   padding: 0 var(--fa-space-3);
-   border: 1px solid var(--fa-line-strong);
-   border-radius: var(--fa-radius-md);
-   color: var(--fa-primary-strong);
-   background: var(--fa-surface);
-   cursor: pointer;
-   font-size: var(--fa-text-base);
-   font-weight: 600;
+   flex-wrap: wrap;
+   gap: var(--fa-space-2);
+   border-radius: var(--fa-radius-lg);
  }


─── apps/web/src/admin/admin.css:0-0 ───
[performance · medium] `.fa-topbar`、`.fa-settings-savebar` 和 `.fa-dialog-overlay` 三处使用了
`backdrop-filter: blur(...)`。此属性在滚动和动画过程中会触发昂贵的 GPU
合成层重绘，在低端设备或老旧浏览器上可能导致明显的卡顿和掉帧。建议评估是否所有场景都必需，或仅在支持硬件加速的设备上启用。



─── apps/web/src/admin/admin.css:0-0 ───
[bug · high] 1024px 断点下的响应式表格卡片布局仅对 `.fa-table` 生效，但
`.fa-audit-table`、`.fa-accounts-table`、`.fa-users-table`、`.fa-user-space-table`、`.fa-feedback-table`
等独立表格类并未被覆盖。这些表格在基础样式中均设置了较大的 `min-width`（如 `.fa-audit-table` 为 65rem），在 768px~1024px
视口下将无法自动转为卡片布局，可能导致水平溢出或布局断裂。



─── apps/web/src/admin/admin.css:3438-3440 ───
[maintainability · low] 审计日志页面（`.fa-audit-*`）拥有独立的 768px 媒体查询断点（位于文件末尾附近），与全局的 1280/1024/768/480
四步断点体系不完全对齐。这种分散的断点定义增加了维护成本，未来修改断点值时需要同时更新多处，容易遗漏。建议将审计页面的响应式逻辑收敛到全局断点中统一管理。



─── apps/web/src/styles.css:1-22 ───
[bug · critical] **:root CSS 自定义属性全部使用 oklch() 颜色函数**，该函数在较旧浏览器中不支持（Chrome < 111, Firefox < 113,
Safari < 16.4）。文件未提供任何降级方案（fallback），在不支持的浏览器中所有使用这些变量的颜色将完全失效（透明/黑色），导致整个站点白屏或无法阅读。

**建议：** 为关键颜色变量添加 fallback，例如 `color: oklch(0.25 0.02 165); color: oklch(0.25 0.02 var(--hue));` 或使用
PostCSS 插件（如 postcss-oklab-function）在构建时生成降级值。

  :root {
-       font-family: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif;
-       font-size: 16px;
-       color: oklch(0.25 0.02 var(--hue));
-       background: oklch(0.95 0.01 var(--hue));
        --hue: 165;
+       /* Fallback hex/rgb values for browsers that don't support oklch() */
+       --primary-fallback: #7fc9b8;
+       --page-bg-fallback: #f2f8f6;
        --primary: oklch(0.7 0.14 var(--hue));
        --page-bg: oklch(0.95 0.01 var(--hue));
-       --card-bg: rgb(255 255 255 / 0.88);
-       --card-bg-solid: #fff;
-       --btn-bg: oklch(0.95 0.025 var(--hue));
-       --btn-hover: oklch(0.9 0.05 var(--hue));
-       --deep-text: oklch(0.25 0.02 var(--hue));
-       --muted-text: rgb(0 0 0 / 0.56);
-       --faint-text: rgb(0 0 0 / 0.38);
-       --line: rgb(0 0 0 / 0.08);
-       --shadow: 0 8px 28px rgb(0 0 0 / 0.04);
-       --radius: 1rem;
-       --page-width: 100rem;
-       --navbar-height: 4.5rem;
-       --ease: cubic-bezier(0.4, 0, 0.2, 1);
+       /* ... other variables */
  }
+
+ /* 在使用这些变量的地方提供服务端或构建时的 fallback */


─── apps/web/src/styles.css:610-612 ───
[maintainability · high] **`.nav-search` 使用了 `!important` 和硬编码色值**（`background: #8c97a4
!important;`），违背了文件其他部分使用 CSS 自定义属性的主题化设计原则。硬编码颜色在深色模式下无法自动适配，且 `!important` 会阻止后续样式覆盖，降低可维护性。

**建议：** 使用 CSS 自定义属性变量替代硬编码色值，并移除 `!important`。

- .nav-search { width: 10rem; background: #8c97a4 !important; color: rgb(255 255 255 / .82); transition: width .28s var(--ease), box-shadow .2s; }
- .nav-search input { color: #fff; }
- .nav-search input::placeholder { color: rgb(255 255 255 / .78); }
+ .nav-search { width: 10rem; background: color-mix(in srgb, var(--primary) 18%, var(--page-bg)); color: var(--deep-text); transition: width .28s var(--ease), box-shadow .2s; }
+ :root.dark .nav-search { background: color-mix(in srgb, var(--primary) 22%, var(--page-bg)); }


─── apps/web/src/styles.css:263-265 ───
[maintainability · high] **大段组件样式被重复定义**（约从第1050行开始的 `/* Public account and discussion surfaces. */`
区块和约从第1180行开始的 `/* Community account and comment surfaces */`
区块）。`.account-entry`、`.account-menu`、`.auth-overlay`、`.auth-dialog`、`.comments-heading`、`.comment-it
em` 等数十个选择器被完整重复定义，后面的定义会完全覆盖前面的。

这不仅造成文件体积膨胀（约200行冗余），而且两个版本之间存在细微差异（如 `.auth-form input` 的 `padding` 不同），会导致运行时行为不一致，极难调试。

**建议：** 删除重复的样式定义区块，仅保留一份。如有样式差异，合并到同一选择器中或用额外的类名区分。

- /* Public account and discussion surfaces. */
- .account-menu-wrap { position: relative; display: inline-flex; }
- .account-entry { display: inline-flex; align-items: center; gap: .38rem; min-height: 2.5rem; padding: .25rem .52rem; border-radius: .62rem; background: transparent; color: var(--muted-text); font-size: .76rem; font-weight: 700; }
+ /* 仅保留一份，删除重复定义 */
+ .account-entry { display: inline-flex; align-items: center; gap: .38rem; min-height: 2.2rem; padding: .25rem .5rem; border: 1px solid transparent; border-radius: .55rem; background: transparent; color: var(--muted-text); font-size: .76rem; font-weight: 700; cursor: pointer; }


─── apps/web/src/styles.css:574-578 ───
[performance · medium] **Hero-waves 动画层使用了大量硬件加速属性**（`will-change: transform`、`backface-visibility:
hidden`、`translate3d`），每个 `.wave-layer` 都独立触发 GPU 合成层。4个 wave 层同时动画，在移动端低端设备上可能导致 GPU
内存压力增大、帧率下降和电池消耗增加。

**建议：** 在移动端 `@media (max-width: 767px)` 中减少 wave 层数量或禁用部分动画层。同时考虑使用 `@media
(prefers-reduced-motion: reduce)` 禁用波行动画。

- .wave-layer { transform-box: fill-box; transform-origin: center; will-change: transform; backface-visibility: hidden; animation: wave-slide 25s cubic-bezier(.5, .5, .45, .5) infinite; }
- .wave-layer-one { opacity: .25; animation-duration: 7s; animation-delay: -2s; }
- .wave-layer-two { opacity: .5; animation-duration: 10s; animation-delay: -3s; }
- .wave-layer-three { opacity: .65; animation-duration: 13s; animation-delay: -4s; }
- .wave-layer-four { opacity: .75; animation-duration: 20s; animation-delay: -5s; }
+ @media (max-width: 767px) {
+       .wave-layer-three, .wave-layer-four { display: none; }
+ }
+ @media (prefers-reduced-motion: reduce) {
+       .wave-layer { animation: none !important; }
+ }


─── apps/web/src/styles.css:0-0 ───
[other · high] **`prefers-reduced-motion` 媒体查询覆盖不完整。** 当前仅对
`wave-layer`、`typewriter-caret`、`wallpaper-fade` 动画做了禁用处理，但以下动画仍然会动：

1. `hero-scroll-bob`（滚动提示持续弹跳）
2. `sakura-fall`（樱花飘落动画）
3. `dynamic-image img` 的 hover 缩放过渡
4. `loading`（骨架屏闪烁动画）
5. `post-cover img` 的 hover 缩放

对有前庭障碍、晕动症的用户，未处理的动画可能导致不适。

**建议：** 在 `prefers-reduced-motion` 块中补充所有动画的禁用规则。

  @media (prefers-reduced-motion: reduce) {
-       *, *::before, *::after { scroll-behavior: auto !important; transition-duration: .01ms !important; animation-duration: .01ms !important; }
+       *, *::before, *::after {
+               scroll-behavior: auto !important;
+               transition-duration: .01ms !important;
+               animation-duration: .01ms !important;
+       }
+       .hero-scroll, .sakura-layer, .dynamic-image img, .post-cover img, .loading-card,
+       .hero-image-outgoing, .hero-image-current.is-crossfading { animation: none !important; transition: none !important; }
  }


─── apps/web/src/styles.css:0-0 ───
[maintainability · medium] **多个 `@media (max-width: 767px)`
响应式断点块分散在文件中**，后定义的规则会覆盖先前的同类规则，导致预期外的布局变化。例如：

- `:root` 的 `font-size` 在两个 `@media (max-width: 767px)` 块中分别被设为 `15px`（约480行）和 `14px`（约560行），最终生效的是
`14px`，开发者可能未意识到前一个规则被覆盖。
- `.top-row`、`.navbar`、`.hero`、`.hero-content`、`.hero-image`、`post-list-home` 等数十个选择器在多个断点块中重复出现。

**建议：** 将同一断点的所有规则合并到同一个 `@media (max-width: 767px)` 块中，或使用 CSS 嵌套预处理（如 SCSS/Sass）来组织。



─── apps/web/src/styles.css:223-223 ───
[performance · low] **`.post-card` 使用了 `content-visibility: visible`**，但 `visible`
是默认值，此声明无实际效果。同时，`.sidebar .card, .content-card, .dynamic-page .card` 也使用了 `content-visibility:
visible` 配合 `contain-intrinsic-size: auto`，同样无意义。

`content-visibility` 的正确用法是用 `auto` 来跳过视口外元素的渲染，提升初始加载性能。这里使用 `visible` 既没有性能收益，还增加了代码冗余。

**建议：** 移除这些无意义的 `content-visibility: visible` 声明。如果希望利用 `contain-intrinsic-size` 的尺寸稳定功能，可单独使用该属性。

- .post-card { position: relative; display: flex; min-height: 0; overflow: hidden; contain: layout paint; content-visibility: visible; }
+ .post-card { position: relative; display: flex; min-height: 0; overflow: hidden; contain: layout paint; }


─── apps/web/src/styles.css:14-15 ───
[other · high] **可访问性对比度不足。** `--muted-text` 定义为 `rgb(0 0 0 / 0.56)`（56% 不透明度黑色），`--faint-text` 定义为
`rgb(0 0 0 / 0.38)`（38% 不透明度黑色）。根据 WCAG 2.1 AA 标准，正文文本需要至少 4.5:1 的对比度，大文本需要 3:1。

- `rgba(0,0,0,0.56)` 在白色背景上的对比度约为
3.0:1，仅勉强达到大文本标准，对正文（`.post-excerpt`、`.comment-body`、`.markdown-body`）来说不达标。
- `rgba(0,0,0,0.38)` 在白色背景上的对比度约为 2.0:1，远低于 AA 标准。

**建议：** 提高 `--muted-text` 到至少 0.65 不透明度，`--faint-text` 到至少 0.48 不透明度，以确保文本可读性。

- --muted-text: rgb(0 0 0 / 0.56);
- --faint-text: rgb(0 0 0 / 0.38);
+ --muted-text: rgb(0 0 0 / 0.66);
+ --faint-text: rgb(0 0 0 / 0.48);


─── apps/web/src/styles.css:741-742 ───
[maintainability · medium] **`.comment-sticker-popover-head` 选择器在文件中被定义了两次**，但样式内容不同。第一次定义中使用了
`display: flex` 和 `justify-content: space-between` 等布局属性，而第二次定义在约第1170行附近。由于 CSS
的层叠规则，后定义的会覆盖前者，可能导致弹出头部布局异常。

**建议：** 合并为一处定义，消除重复。



─── stop.ps1:28-34 ───
[maintainability · medium] `try` 块中的 `ConvertFrom-Json` 若解析失败（如 JSON 文件损坏），异常会向外传播，但 `finally`
块仍会删除状态文件，导致诊断信息丢失，不利于排查问题。建议在 `catch` 中记录错误信息后再决定是否删除文件。

  try {
        $state = Get-Content $StatePath -Raw | ConvertFrom-Json
        Stop-TrackedProcess ([int]$state.ApiPid) "@firefly-rebuild/api"
        Stop-TrackedProcess ([int]$state.WebPid) "@firefly-rebuild/web"
+ } catch {
+       Write-Warning "Failed to parse state file: $_"
+       # 保留文件以便人工诊断
+       # Remove-Item $StatePath -Force -ErrorAction SilentlyContinue
+       throw
  } finally {
-       Remove-Item $StatePath -Force -ErrorAction SilentlyContinue
+       # 仅在成功时删除
+       if ($?) { Remove-Item $StatePath -Force -ErrorAction SilentlyContinue }
  }


─── start.ps1:172-176 ───
[bug · high] **启动进程后缺少健康检查**

脚本通过 `Start-Process` 启动 API 和 Web
开发服务器后，直接写入成功信息并退出，未验证两个进程是否真正启动成功或仍在运行。若任一进程因配置错误、端口冲突或编译失败等原因立即崩溃，用户仍会看到 "Firefly Headless
started." 的成功提示，导致后续访问失败时难以排查。

**建议**: 在启动进程后增加健康检查逻辑，例如轮询 API 的 `/health` 端点或检查进程是否仍在运行，超时后给出明确失败提示。

- Write-Host "`nFirefly Headless started." -ForegroundColor Green
- Write-Host "Web:   $($state.WebUrl)"
- Write-Host "API:   $($state.ApiUrl)"
- Write-Host "Logs:  $LogDirectory"
- Write-Host "Stop:  .\stop.ps1"
+ # 在启动进程后添加健康检查
+ $healthCheckTimeout = 30
+ $checkInterval = 2
+ $elapsed = 0
+ while ($elapsed -lt $healthCheckTimeout) {
+     Start-Sleep -Seconds $checkInterval
+     $elapsed += $checkInterval
+     $apiProcess = Get-Process -Id $apiProcess.Id -ErrorAction SilentlyContinue
+     $webProcess = Get-Process -Id $webProcess.Id -ErrorAction SilentlyContinue
+     if (-not $apiProcess -or -not $webProcess) {
+         Write-Warning "One or more processes have exited prematurely. Check logs: $LogDirectory"
+         break
+     }
+     # 可选：尝试 HTTP 健康检查
+     try {
+         $response = Invoke-WebRequest -Uri "http://localhost:5180/health" -TimeoutSec 2 -ErrorAction Stop
+         if ($response.StatusCode -eq 200) { break }
+     } catch {
+         # 服务尚未就绪，继续等待
+     }
+ }


─── start.ps1:131-132 ───
[security · medium] **硬编码 MySQL root 密码存在安全风险**

在 `DockerMySql` 分支中，硬编码设置了 `$env:MYSQL_PASSWORD = "firefly_root_dev"`，且使用的是 `root`
用户。虽然注释说明这是开发环境，但硬编码 root 密码（尤其是提交到代码仓库）仍存在以下风险：
1. 密码泄露后，若开发环境暴露在网络上，数据库可被直接访问
2. 部分开发者可能忽略生产环境复用此密码
3. 违反最小权限原则——应使用专用应用用户而非 root

**建议**: 从 `.env` 文件读取密码，或生成随机密码并输出到控制台。同时建议使用专有的数据库用户而非 root。

- $env:MYSQL_USER = "root"
- $env:MYSQL_PASSWORD = "firefly_root_dev"
+ # 从 .env 文件读取或随机生成密码
+ $env:MYSQL_USER = "firefly_app"
+ $env:MYSQL_PASSWORD = if (Test-Path $EnvironmentPath) {
+     $envContent = Get-Content $EnvironmentPath | Where-Object { $_ -match '^MYSQL_PASSWORD=' }
+     if ($envContent) { $envContent -replace '^MYSQL_PASSWORD=', '' } else { "firefly_dev_$(Get-Random)" }
+ } else { "firefly_dev_$(Get-Random)" }


─── start.ps1:156-161 ───
[maintainability · low] **进程启动使用重定向标准输出/错误，但未设置进程编码，中文日志可能乱码**

`Start-Process` 通过 `-RedirectStandardOutput` 和 `-RedirectStandardError` 重定向输出，但 PowerShell
控制台输出重定向到文件时默认使用系统的活动代码页编码。如果 Node.js/pnpm 输出包含中文或 Unicode 字符，日志文件可能出现乱码。

**建议**: 在重定向输出时指定编码，或确保输出编码为 UTF-8。

- $apiProcess = Start-Process -FilePath $script:PnpmPath `
-       -ArgumentList @($script:PnpmPrefix + @("--filter", "@firefly-rebuild/api", "dev")) `
-       -WorkingDirectory $Root -RedirectStandardOutput $apiLog -RedirectStandardError $apiErrorLog -PassThru
- $webProcess = Start-Process -FilePath $script:PnpmPath `
-       -ArgumentList @($script:PnpmPrefix + @("--filter", "@firefly-rebuild/web", "dev")) `
-       -WorkingDirectory $Root -RedirectStandardOutput $webLog -RedirectStandardError $webErrorLog -PassThru
+ # 在脚本开头设置 UTF-8 编码
+ $PSDefaultParameterValues['*:Encoding'] = 'utf8'
+ # 或在启动进程前
+ $env:PNPM_HOME = $env:PNPM_HOME
+ # 确保控制台编码为 UTF-8
+ [Console]::OutputEncoding = [System.Text.UTF8Encoding]::new()


─── start.ps1:117-123 ───
[maintainability · medium] **Docker MySQL 健康检查循环中，若 `docker compose ps -q mysql`
返回空（容器未启动），`$health` 为 `$null`，后续 `$health -match` 仍可正常工作，但不会输出任何提示信息，用户难以判断等待进度**

在 2 分钟的超时等待循环中，没有任何进度提示输出，用户只能看到 "Waiting for Docker MySQL readiness"
后长时间无响应，无法判断是在等待还是已经卡死。另外，若容器启动失败（如端口冲突），`docker compose ps -q mysql` 返回空，循环会一直等到超时才报错，用户体验较差。

**建议**: 在循环中增加进度提示，并检查容器是否仍在运行。

  Write-Step "Waiting for Docker MySQL readiness"
  $deadline = (Get-Date).AddMinutes(2)
  while ((Get-Date) -lt $deadline) {
-       $health = (& docker compose ps -q mysql | ForEach-Object { docker inspect --format '{{.State.Health.Status}}' $_ })
-       if ($health -match "healthy") { break }
+       $containerId = & docker compose ps -q mysql 2>$null
+       if (-not $containerId) {
+               Write-Warning "MySQL container is not running. Check docker compose logs."
+               Start-Sleep -Seconds 5
+               continue
+       }
+       $health = $containerId | ForEach-Object { docker inspect --format '{{.State.Health.Status}}' $_ }
+       Write-Host "." -NoNewline
+       if ($health -match "healthy") {
+               Write-Host " OK" -ForegroundColor Green
+               break
+       }
        Start-Sleep -Seconds 2
  }


─── start.ps1:0-0 ───
[maintainability · low] **脚本未恢复原始工作目录，影响调用者体验**

脚本在第 65 行执行 `Set-Location $Root` 后，直到脚本结束都没有恢复原始工作目录。如果用户从其他目录调用此脚本（例如
`./scripts/start.ps1`），脚本结束后当前工作目录被永久改变，可能导致用户后续操作混乱。

**建议**: 保存原始目录并在脚本结束时恢复。

+ $originalLocation = Get-Location
+ try {
  Set-Location $Root
- ...
- # 脚本结束前未恢复原始目录
+     # ... 其余脚本逻辑 ...
+ } finally {
+     Set-Location $originalLocation
+ }


─── start.ps1:128-132 ───
[maintainability · medium] **Docker MySQL 设置的环境变量在脚本退出后残留，影响同一会话的后续命令**

当使用 `-DockerMySql` 参数时，脚本通过 `$env:MYSQL_HOST`、`$env:MYSQL_PORT` 等设置进程级环境变量。这些变量在脚本结束后仍然存在当前
PowerShell 会话中，后续在该会话中运行的其他命令（如 `stop.ps1`、`pnpm` 命令等）可能会意外使用这些残留的数据库配置，导致混淆或错误。

**建议**: 使用 `try/finally` 块确保在脚本结束时清理这些环境变量，或使用局部作用域（如脚本块）来隔离环境变量修改。

- $env:MYSQL_HOST = "127.0.0.1"
- $env:MYSQL_PORT = "3307"
- $env:MYSQL_DATABASE = "firefly_blog"
- $env:MYSQL_USER = "root"
- $env:MYSQL_PASSWORD = "firefly_root_dev"
+ # 保存原始值并注册清理
+ $originalMySqlEnv = @{}
+ foreach ($key in @('MYSQL_HOST','MYSQL_PORT','MYSQL_DATABASE','MYSQL_USER','MYSQL_PASSWORD')) {
+     $originalMySqlEnv[$key] = [Environment]::GetEnvironmentVariable($key, 'Process')
+     ${env:$key} = ...
+ }
+ try {
+     # ... 主逻辑 ...
+ } finally {
+     foreach ($key in $originalMySqlEnv.Keys) {
+         if ($originalMySqlEnv[$key] -ne $null) {
+             [Environment]::SetEnvironmentVariable($key, $originalMySqlEnv[$key], 'Process')
+         } else {
+             [Environment]::SetEnvironmentVariable($key, $null, 'Process')
+         }
+     }
+ }


─── start.ps1:43-54 ───
[bug · high] **停止进程时命令行匹配逻辑不准确，且 `Get-CimInstance` 失败时无声返回**

`Stop-TrackedProcess` 使用 `-like` 通配符匹配 `$commandLine` 与 `$ExpectedCommand`，但存在以下问题：
1. 路径分隔符不匹配：`$ExpectedCommand` 使用正斜杠 `/`，而 Windows 实际路径使用反斜杠 `\`，导致 `-notlike` 误判为不匹配，进程被跳过。
2. 通配符元字符风险：若 `$Root` 或 `$ExpectedCommand` 包含 `[`、`?` 等通配符元字符，`-like` 会将其解释为模式而非字面量，导致匹配异常。
3. `Get-CimInstance` 可能因权限不足或 WMI 服务不可用而返回 `$null`，此时函数直接返回，调用方无法收到警告，导致进程无法停止。

**建议**: 使用 `-match` 运算符（正则表达式）或 `$commandLine.Contains($ExpectedCommand)` 进行精确的子串匹配；同时增加对
`Get-CimInstance` 失败场景的日志输出（如 `Write-Warning`）。

  function Stop-TrackedProcess([int]$ProcessId, [string]$ExpectedCommand) {
        if ($ProcessId -le 0) { return }
        $processInfo = Get-ProcessInfo $ProcessId
-       if (-not $processInfo) { return }
+       if (-not $processInfo) {
+               Write-Warning "Could not get process info for PID $ProcessId (WMI/CIM query returned nothing). Process may have already exited."
+               return
+       }
        $commandLine = [string]$processInfo.CommandLine
-       if (($commandLine -notlike "*$Root*") -and ($commandLine -notlike "*$ExpectedCommand*")) {
+       if (-not $commandLine.Contains($Root) -and -not $commandLine.Contains($ExpectedCommand)) {
                Write-Warning "Skipping PID ${ProcessId}: the command line does not match Firefly Headless."
                return
        }
        Write-Host "Stopping PID $ProcessId ..." -ForegroundColor Yellow
        & taskkill.exe /PID $ProcessId /T /F | Out-Null
  }


─── start.ps1:0-0 ───
[performance · medium] **端口检查存在竞态条件且 `Get-NetTCPConnection` 可能不可用**

`Test-PortAvailable` 依赖 `Get-NetTCPConnection` 查询监听状态，但存在以下问题：
1. 竞态条件：检查和启动进程之间存在时间窗口，其他进程可能在这期间占用端口。
2. `Get-NetTCPConnection` 在某些 Windows 精简安装或 Nano Server 环境中可能不可用（cmdlet 不存在），此时 `-ErrorAction
SilentlyContinue` 会抑制错误，函数返回 `$true`（端口可用），造成假阳性，后续启动可能因端口冲突失败。

**建议**: 增加重试机制，并在启动进程后二次确认端口已被占用；同时增加备用检测机制，如使用 .NET `TcpListener` 尝试绑定端口，或使用 `netstat` 命令作为回退方案。

  function Test-PortAvailable([int]$Port) {
-       $listener = Get-NetTCPConnection -LocalPort $Port -State Listen -ErrorAction SilentlyContinue
+     try {
+         $listener = Get-NetTCPConnection -LocalPort $Port -State Listen -ErrorAction Stop
        return -not [bool]$listener
+     } catch {
+         # Get-NetTCPConnection may fail due to insufficient permissions or on older Windows
+         # Fall back to .NET approach
+         try {
+             $tcpListener = [System.Net.Sockets.TcpListener]::new([System.Net.IPAddress]::Loopback, $Port)
+             $tcpListener.Start()
+             $tcpListener.Stop()
+             return $true
+         } catch {
+             return $false
  }
- ...
- if (-not (Test-PortAvailable 5174)) {
-       throw "Port 5174 is already in use..."
  }
- if (-not (Test-PortAvailable 5180)) {
-       throw "Port 5180 is already in use..."
  }


─── deploy/manage.sh:461-473 ───
[security · high] **临时文件写入密码后尚未注册清理列表，存在安全隐患**

`make_mysql_defaults_file` 函数中，先 `mktemp` 创建临时文件，然后 `chmod 600`，再写入包含数据库密码的敏感内容，最后才将文件路径加入
`SECRET_TEMP_FILES` 清理数组。若在写入内容后、注册到清理列表之前的间隙进程崩溃（例如被 SIGKILL 终止），临时文件中的密码将残留在磁盘上，不会被
`cleanup_secret_files` 清理。

建议在 `mktemp` 之后立即将文件路径加入 SECRET_TEMP_FILES，确保任何时候崩溃都能被清理。

  make_mysql_defaults_file() {
    local output_var="$1" user="$2" password="$3" host="$4" port="$5" file
    file="$(mktemp /tmp/firefly-mysql.XXXXXX)"
+   SECRET_TEMP_FILES+=("${file}")  # 立即注册，防止崩溃时泄露
    chmod 600 "${file}"
    {
      printf '[client]\n'
      printf 'user=%s\n' "$(mysql_option_value "${user}")"
      printf 'password=%s\n' "$(mysql_option_value "${password}")"
      printf 'host=%s\n' "$(mysql_option_value "${host}")"
      printf 'port=%s\n' "$(mysql_option_value "${port}")"
      printf 'protocol=tcp\n'
    } > "${file}"
-   SECRET_TEMP_FILES+=("${file}")


─── deploy/manage.sh:960-968 ───
[security · high] **管理员密码通过管道传递，可能被其他进程窥探**

`set_initial_admin_password` 中使用 `printf '%s' "${ADMIN_PASSWORD}" | pnpm ...`
通过管道将密码明文传递给子进程。虽然管道内容不直接出现在 `/proc/pid/cmdline` 中，但密码仍存在于父进程的地址空间中，且子进程的 stdin 可被其他具有足够权限的进程通过
`/proc/pid/fd/0` 读取。

建议使用临时文件传递密码（写入后立即设置 600 权限）或通过环境变量传递（子进程需自行清理）。

  set_initial_admin_password() {
    valid_admin_password "${ADMIN_PASSWORD}" || die "未提供有效的初始管理员密码。"
    info "设置初始管理员密码。"
-   (cd "${APP_DIR}" && printf '%s' "${ADMIN_PASSWORD}" | pnpm --filter @firefly-rebuild/api exec tsx src/cli.ts set-admin-password)
+   local pw_file
+   pw_file="$(mktemp /tmp/firefly-admin-pw.XXXXXX)"
+   SECRET_TEMP_FILES+=("${pw_file}")
+   chmod 600 "${pw_file}"
+   printf '%s' "${ADMIN_PASSWORD}" > "${pw_file}"
+   (cd "${APP_DIR}" && pnpm --filter @firefly-rebuild/api exec tsx src/cli.ts set-admin-password < "${pw_file}")
    ADMIN_PASSWORD=""
    if database_needs_admin_password; then
      die "管理员密码写入后校验失败。"
    fi
  }


─── deploy/manage.sh:0-0 ───
[security · medium] **锁文件路径存在符号链接攻击风险**

`LOCK_FILE="/var/lock/firefly-react-deploy.lock"`，`acquire_lock` 中通过 `exec 9>"${LOCK_FILE}"`
打开文件描述符。如果 `/var/lock` 目录权限配置不当，或攻击者能在 `mkdir -p` 执行后、`exec 9>` 执行前创建符号链接，则可能将锁文件指向任意系统文件，导致以 root
身份覆盖该文件。

虽然脚本以 root 运行且 `/var/lock` 通常权限正确，但建议使用 `flock` 的目录描述符锁定方式，或先在安全的临时目录创建锁文件，再通过 `flock` 锁定。

  LOCK_FILE="/var/lock/firefly-react-deploy.lock"

  acquire_lock() {
    [[ "${LOCK_ACQUIRED}" == true ]] && return
    if ! command -v flock >/dev/null 2>&1; then
      info "缺少 flock，正在自动安装 util-linux。"
      require_supported_os
      export DEBIAN_FRONTEND=noninteractive
      apt-get update
      apt-get install -y --no-install-recommends util-linux
      command -v flock >/dev/null 2>&1 || die "util-linux 安装后仍找不到 flock。"
    fi
    mkdir -p "$(dirname "${LOCK_FILE}")"
+   # 使用 O_NOFOLLOW 防止符号链接攻击：先创建临时文件再原子重命名
+   local tmp_lock
+   tmp_lock="$(mktemp "$(dirname "${LOCK_FILE}")/.firefly-lock.XXXXXX")"
+   mv -f "${tmp_lock}" "${LOCK_FILE}" 2>/dev/null || true
    exec 9>"${LOCK_FILE}"
    flock -n 9 || die "已有另一个部署操作正在运行。"
    LOCK_ACQUIRED=true
  }


─── deploy/manage.sh:0-0 ───
[maintainability · medium] **ERR trap 与 `die` 函数可能产生重复的错误消息**

脚本同时设置了 `set -Eeuo pipefail` 和 `trap 'error "操作失败（第 ${LINENO} 行）。"' ERR`。当 `die` 被调用时，`die` 自身已通过
`error` 打印了具体的错误原因（如 "ERROR: 应用目录不存在"），然后 `exit 1` 退出。在某些 Bash 版本和上下文下，`exit 1` 可能触发 ERR
trap，导致再打印一条 "ERROR: 操作失败（第 XXX 行）。" 的通用消息，造成重复且混淆。

建议：ERR trap 改为仅在不希望静默失败的关键位置设置，或移除 ERR trap 的重复输出，让 `die` 作为唯一的错误输出源。

- trap 'error "操作失败（第 ${LINENO} 行）。"' ERR
+ # 方案一：移除 ERR trap，依赖 die 和 set -e 的错误处理
+ # trap 'error "操作失败（第 ${LINENO} 行）。"' ERR  # 移除此行

- die() { error "$*"; exit 1; }
+ # 方案二：如果保留 ERR trap，让 die 设置标志避免重复输出
+ DIE_CALLED=false
+ die() { DIE_CALLED=true; error "$*"; exit 1; }
+ trap '[[ "${DIE_CALLED}" == true ]] || error "操作失败（第 ${LINENO} 行）。"' ERR


─── deploy/manage.sh:104-110 ───
[security · medium] **`load_config` 中 `printf -v` 的键名仅依赖正则和 case 过滤，存在风险**

`load_config` 从配置文件中读取键值对，通过 `printf -v "${key}" '%s' "${value}"` 动态赋值给变量。虽然 `case`
语句限制了写入的变量名范围，但存在以下风险：
1. 配置文件中若出现 `APP_DIR` 等受控变量之外的恶意构造键名，但通过了 `^[A-Z0-9_]+$` 正则，且未来有开发者向 case 列表添加了新变量而忘记初始化，则可能被写入意外值。
2. 若配置文件中存在 `PATH`、`LD_PRELOAD` 等环境变量名（虽然不匹配 `^[A-Z0-9_]+$` 正则全大写规则，但严格来说也应防范），可能造成安全风险。

建议：对 `case` 中每个变量进行白名单赋值，而非使用 `printf -v` 动态变量名。

  while IFS='=' read -r key value || [[ -n "${key}" ]]; do
      [[ "${key}" =~ ^[A-Z0-9_]+$ ]] || continue
      case "${key}" in
-       APP_DIR|REPO_URL|BRANCH|DOMAIN|API_PORT|APP_USER|NODE_MAJOR|NGINX_SITE|DB_MODE|MYSQL_HOST|MYSQL_PORT|MYSQL_DATABASE|MYSQL_USER|SSL_EMAIL|PUBLIC_SCHEME)
-         printf -v "${key}" '%s' "${value}" ;;
+       APP_DIR) APP_DIR="${value}" ;;
+       REPO_URL) REPO_URL="${value}" ;;
+       BRANCH) BRANCH="${value}" ;;
+       DOMAIN) DOMAIN="${value}" ;;
+       API_PORT) API_PORT="${value}" ;;
+       APP_USER) APP_USER="${value}" ;;
+       NODE_MAJOR) NODE_MAJOR="${value}" ;;
+       NGINX_SITE) NGINX_SITE="${value}" ;;
+       DB_MODE) DB_MODE="${value}" ;;
+       MYSQL_HOST) MYSQL_HOST="${value}" ;;
+       MYSQL_PORT) MYSQL_PORT="${value}" ;;
+       MYSQL_DATABASE) MYSQL_DATABASE="${value}" ;;
+       MYSQL_USER) MYSQL_USER="${value}" ;;
+       SSL_EMAIL) SSL_EMAIL="${value}" ;;
+       PUBLIC_SCHEME) PUBLIC_SCHEME="${value}" ;;
      esac
  done < "${CONFIG_FILE}"


─── deploy/manage.sh:1053-1083 ───
[performance · medium] **`run_configure` 中每次配置修改都会触发完整重装和重构建**

`run_configure` 调用 `install_base_dependencies`、`install_node_pnpm`、`install_and_build_application`
等函数，即使仅修改了数据库密码或域名等配置项，也会重新安装系统依赖、Node.js、pnpm、以及完整的前后端构建。这在生产环境中对于频繁的配置调整会造成不必要的停机时间。

建议：分离配置变更与完整重装的逻辑，仅在配置变更涉及系统依赖时重装，否则只更新 .env、Nginx 配置和重启服务。

  run_configure() {
    collect_settings
    normalize_public_scheme
    show_summary
    if ! confirm_action; then
      warn "已取消配置。"
      return
    fi

-   install_base_dependencies
-   install_node_pnpm
+   # 仅当系统依赖变更时才重装
    ensure_service_user
    [[ -d "${APP_DIR}/.git" ]] || die "部署目录尚无应用代码，请先运行 first。"
    prepare_database
    if database_needs_admin_password; then
      warn "目标数据库尚无 admin 管理员，需要设置初始密码。"
      prompt_admin_password
    fi
    write_config
    write_application_env
    prepare_persistent_directories
-   install_and_build_application
+   # 仅重新构建，不重装依赖
+   info "按锁文件安装项目依赖。"
+   (cd "${APP_DIR}" && NODE_ENV=development pnpm install --frozen-lockfile)
+   (cd "${APP_DIR}" && VITE_API_ORIGIN='' pnpm build)
    if [[ -n "${ADMIN_PASSWORD}" ]]; then
      set_initial_admin_password
    fi
    write_systemd_unit
    write_nginx_config
    restart_application
    verify_public_proxy
    info "配置已保存并应用。"
  }


─── deploy/manage.sh:330-337 ───
[bug · medium] **`require_supported_os` 中 `source /etc/os-release` 若失败会导致脚本静默退出**

`require_supported_os` 函数中先检查 `[[ -r /etc/os-release ]]`，但随后 `source /etc/os-release`
可能在文件存在但语法错误时失败。由于设置了 `set -e`，`source` 失败会直接导致脚本退出，且触发 ERR trap 输出通用错误消息 "操作失败（第 XXX
行）。"，而非具体的诊断信息，使排查困难。

建议：用 `source /etc/os-release || die "无法解析 /etc/os-release"` 包裹。

  require_supported_os() {
    [[ -r /etc/os-release ]] || die "仅支持 Debian/Ubuntu systemd 服务器。"
    # shellcheck disable=SC1091
-   source /etc/os-release
+   source /etc/os-release || die "无法解析 /etc/os-release，文件可能已损坏。"
    case "${ID:-}:${ID_LIKE:-}" in
      debian:*|ubuntu:*|*:debian*) ;;
      *) die "当前系统不受支持，仅支持 Debian/Ubuntu。" ;;
    esac


─── database/schema.sql:361-363 ───
[security · critical] **安全风险：硬编码默认管理员密码哈希**

`admin_users` 表的 INSERT 语句中硬编码了一个密码哈希值，标签为
`firefly-default`。这意味着该哈希对应的明文密码是项目公开的默认密码，任何知晓该默认密码的人都可以以 `superadmin` 身份登录系统。

尽管密码是哈希存储而非明文，但 `firefly-default` 标签表明这是一个已知的默认密码，构成了严重的安全隐患。

**建议：**
1. 移除或注释掉该 INSERT 语句，改为在首次部署时通过 CLI 交互式设置管理员密码。
2. 如果必须保留自动初始化逻辑，应生成一个随机密码并在控制台输出，而不是硬编码已知密码。

- INSERT INTO admin_users (username, display_name, password_hash, role, is_active)
- VALUES ('admin', '系统管理员', 'scrypt$firefly-default$525f930380e711523b2016fa790daf89978f8a0acc013041610bf5fbbb08230a309c4ba12cff06b493148e9ccdc7bdbd6ebd8bca1038b9c1f8435904a26712cd', 'superadmin', TRUE)
- ON DUPLICATE KEY UPDATE username = username;
+ -- 移除硬编码默认密码，改为通过 CLI 或初始化脚本交互式创建管理员账户
+ -- 或使用随机生成密码并在首次部署时输出：
+ -- INSERT INTO admin_users (username, display_name, password_hash, role, is_active)
+ -- SELECT 'admin', '系统管理员', SHA2(RANDOM_PASSWORD, 256), 'superadmin', TRUE
+ -- WHERE NOT EXISTS (SELECT 1 FROM admin_users WHERE username = 'admin');


─── database/schema.sql:263-263 ───
[bug · high] **数据完整性缺陷：`comments.user_id` 缺少外键约束**

通过 ALTER TABLE 添加 `comments.user_id` 列时，没有添加 FOREIGN KEY 约束引用 `comment_users(id)`。这会导致：
1. `user_id` 可以指向不存在的 `comment_users` 记录，产生悬挂引用。
2. 当对应的 `comment_users` 记录被删除时，`comments` 表中的引用不会自动置 NULL 或级联删除。

**建议：** 在添加 `user_id` 列的 ALTER TABLE 语句中增加外键约束，`ON DELETE SET NULL` 是比较安全的选择。

- SET @firefly_comment_user_id_sql = IF((SELECT COUNT(*) FROM information_schema.COLUMNS WHERE TABLE_SCHEMA = DATABASE() AND TABLE_NAME = 'comments' AND COLUMN_NAME = 'user_id') = 0, 'ALTER TABLE comments ADD COLUMN user_id BIGINT UNSIGNED NULL AFTER post_id', 'SELECT 1');
+ SET @firefly_comment_user_id_sql = IF((SELECT COUNT(*) FROM information_schema.COLUMNS WHERE TABLE_SCHEMA = DATABASE() AND TABLE_NAME = 'comments' AND COLUMN_NAME = 'user_id') = 0, 'ALTER TABLE comments ADD COLUMN user_id BIGINT UNSIGNED NULL AFTER post_id, ADD CONSTRAINT fk_comments_user FOREIGN KEY (user_id) REFERENCES comment_users(id) ON DELETE SET NULL', 'SELECT 1');


─── database/schema.sql:265-265 ───
[bug · high] **数据完整性缺陷：`comments.parent_id` 缺少外键约束**

通过 ALTER TABLE 添加 `comments.parent_id` 列时，没有添加 FOREIGN KEY 约束引用 `comments(id)`。这会导致：
1. `parent_id` 可以指向不存在的评论记录，产生悬挂引用。
2. 当父评论被删除时，子评论的 `parent_id` 不会自动置 NULL，导致指向已删除的评论。

**建议：** 在添加 `parent_id` 列的 ALTER TABLE 语句中增加自引用外键约束，`ON DELETE SET NULL` 是比较安全的选择。

- SET @firefly_comment_parent_id_sql = IF((SELECT COUNT(*) FROM information_schema.COLUMNS WHERE TABLE_SCHEMA = DATABASE() AND TABLE_NAME = 'comments' AND COLUMN_NAME = 'parent_id') = 0, 'ALTER TABLE comments ADD COLUMN parent_id BIGINT UNSIGNED NULL AFTER user_id', 'SELECT 1');
+ SET @firefly_comment_parent_id_sql = IF((SELECT COUNT(*) FROM information_schema.COLUMNS WHERE TABLE_SCHEMA = DATABASE() AND TABLE_NAME = 'comments' AND COLUMN_NAME = 'parent_id') = 0, 'ALTER TABLE comments ADD COLUMN parent_id BIGINT UNSIGNED NULL AFTER user_id, ADD CONSTRAINT fk_comments_parent FOREIGN KEY (parent_id) REFERENCES comments(id) ON DELETE SET NULL', 'SELECT 1');


─── database/schema.sql:4-16 ───
[maintainability · medium] **维护性问题：`authors.profile` 列仅存在于迁移脚本中，未包含在 CREATE TABLE 中**

`authors` 表的 CREATE TABLE 语句中没有包含 `profile` 列，而是通过后续的 ALTER TABLE
迁移脚本添加。对于首次创建数据库的情况，这虽然能正常工作（迁移脚本紧随 CREATE TABLE 之后执行），但：
1. 任何阅读该 Schema 文件的人都会认为 `profile` 列不存在，除非注意到迁移部分。
2. 如果迁移脚本被修改或删除，新数据库将永久缺少该列。

**建议：** 将 `profile` 列直接添加到 `authors` 的 CREATE TABLE 定义中，以保持一致性。

  CREATE TABLE IF NOT EXISTS authors (
    id BIGINT UNSIGNED NOT NULL AUTO_INCREMENT PRIMARY KEY,
    name VARCHAR(120) NOT NULL,
    bio VARCHAR(255) NOT NULL DEFAULT '',
    avatar VARCHAR(500) NOT NULL DEFAULT '',
    email VARCHAR(190) NULL,
    github_url VARCHAR(500) NULL,
    qq_url VARCHAR(500) NULL,
    rss_url VARCHAR(500) NULL,
    links JSON NOT NULL DEFAULT (JSON_ARRAY()),
+   profile JSON NOT NULL DEFAULT (JSON_OBJECT()),
    created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
  ) ENGINE=InnoDB;


─── database/schema.sql:88-89 ───
[performance · medium] **性能问题：`INSERT IGNORE INTO post_categories` 在每次执行 schema 时都会运行**

迁移脚本中的 `INSERT IGNORE INTO post_categories ... SELECT id, category_id FROM posts WHERE category_id
IS NOT NULL` 语句位于 `CREATE TABLE IF NOT EXISTS` 之外，没有包裹在条件判断中。这意味着每次执行该 schema 文件时，都会全表扫描 `posts`
表并尝试插入 `post_categories`。对于已迁移过的数据库，这是完全没有必要的 I/O 开销。

**建议：** 将此迁移语句包裹在条件判断中，比如检查 `post_categories` 表是否为空，或者使用类似其他迁移语句的 `IF ... SELECT COUNT(*)` 模式。

- INSERT IGNORE INTO post_categories (post_id, category_id)
-   SELECT id, category_id FROM posts WHERE category_id IS NOT NULL;
+ SET @firefly_post_categories_migrate_sql = IF(
+   (SELECT COUNT(*) FROM information_schema.TABLES WHERE TABLE_SCHEMA = DATABASE() AND TABLE_NAME = 'post_categories') > 0
+   AND (SELECT COUNT(*) FROM post_categories) = 0,
+   'INSERT IGNORE INTO post_categories (post_id, category_id) SELECT id, category_id FROM posts WHERE category_id IS NOT NULL',
+   'SELECT 1'
+ );
+ PREPARE firefly_post_categories_migrate_statement FROM @firefly_post_categories_migrate_sql;
+ EXECUTE firefly_post_categories_migrate_statement;
+ DEALLOCATE PREPARE firefly_post_categories_migrate_statement;


─── database/schema.sql:285-285 ───
[bug · high] **数据完整性缺陷：`comments.sticker_id` 缺少外键约束**

通过 ALTER TABLE 添加的 `sticker_id` 列没有添加 FOREIGN KEY 约束引用 `comment_stickers(id)`，导致 `sticker_id`
可以指向不存在的表情贴纸记录。

**建议：** 在 ALTER TABLE 语句中添加外键约束，`ON DELETE SET NULL` 是合适的选择。

- SET @firefly_comment_sticker_id_sql = IF((SELECT COUNT(*) FROM information_schema.COLUMNS WHERE TABLE_SCHEMA = DATABASE() AND TABLE_NAME = 'comments' AND COLUMN_NAME = 'sticker_id') = 0, 'ALTER TABLE comments ADD COLUMN sticker_id BIGINT UNSIGNED NULL', 'SELECT 1');
+ SET @firefly_comment_sticker_id_sql = IF((SELECT COUNT(*) FROM information_schema.COLUMNS WHERE TABLE_SCHEMA = DATABASE() AND TABLE_NAME = 'comments' AND COLUMN_NAME = 'sticker_id') = 0, 'ALTER TABLE comments ADD COLUMN sticker_id BIGINT UNSIGNED NULL, ADD CONSTRAINT fk_comments_sticker FOREIGN KEY (sticker_id) REFERENCES comment_stickers(id) ON DELETE SET NULL', 'SELECT 1');


─── database/schema.sql:287-287 ───
[bug · high] **数据完整性缺陷：`comments.personal_sticker_id` 缺少外键约束**

通过 ALTER TABLE 添加的 `personal_sticker_id` 列没有添加 FOREIGN KEY 约束引用 `comment_user_stickers(id)`，导致
`personal_sticker_id` 可以指向不存在的用户自定义表情贴纸记录。

**建议：** 在 ALTER TABLE 语句中添加外键约束，`ON DELETE SET NULL` 是合适的选择。

- SET @firefly_comment_personal_sticker_id_sql = IF((SELECT COUNT(*) FROM information_schema.COLUMNS WHERE TABLE_SCHEMA = DATABASE() AND TABLE_NAME = 'comments' AND COLUMN_NAME = 'personal_sticker_id') = 0, 'ALTER TABLE comments ADD COLUMN personal_sticker_id BIGINT UNSIGNED NULL', 'SELECT 1');
+ SET @firefly_comment_personal_sticker_id_sql = IF((SELECT COUNT(*) FROM information_schema.COLUMNS WHERE TABLE_SCHEMA = DATABASE() AND TABLE_NAME = 'comments' AND COLUMN_NAME = 'personal_sticker_id') = 0, 'ALTER TABLE comments ADD COLUMN personal_sticker_id BIGINT UNSIGNED NULL, ADD CONSTRAINT fk_comments_personal_sticker FOREIGN KEY (personal_sticker_id) REFERENCES comment_user_stickers(id) ON DELETE SET NULL', 'SELECT 1');


─── database/schema.sql:87-87 ───
[performance · medium] **性能问题：`UPDATE posts SET filename = slug` 在每次执行 schema 时都会运行**

该语句没有包裹在任何条件判断中，意味着每次执行 schema 文件时，都会全表扫描 `posts` 表并更新所有 `filename` 为空或 NULL 的行。对于已迁移过的数据库，绝大多数行的
`filename` 已经不为空，但仍然会执行全表扫描和一大部分无意义的更新操作。

**建议：** 将其包裹在条件判断中，比如检查是否有任何行的 `filename` 仍为空或 NULL。

- UPDATE posts SET filename = slug WHERE filename = '' OR filename IS NULL;
+ SET @firefly_post_filename_update_sql = IF(
+   (SELECT COUNT(*) FROM posts WHERE filename = '' OR filename IS NULL) > 0,
+   'UPDATE posts SET filename = slug WHERE filename = '''' OR filename IS NULL',
+   'SELECT 1'
+ );
+ PREPARE firefly_post_filename_update_statement FROM @firefly_post_filename_update_sql;
+ EXECUTE firefly_post_filename_update_statement;
+ DEALLOCATE PREPARE firefly_post_filename_update_statement;


─── database/schema.sql:267-267 ───
[bug · low] **潜在问题：`comments` 表 ALTER 迁移中 `author_avatar` 列添加在 `author_name` 之后，但初始 CREATE TABLE 中
`author_name` 是第2列**

`ALTER TABLE comments ADD COLUMN author_avatar VARCHAR(512) NOT NULL DEFAULT '' AFTER author_name` 在
`author_name` 之后添加 `author_avatar`，但初始 CREATE TABLE 中 `author_name` 是第2列（`id` 之后）。如果 `comments`
表的列顺序与预期不符，`AFTER author_name` 可能将新列放在意料之外的位置。不过由于 ALTER TABLE 自带存在性检查，这不会导致错误，只是列顺序可能不符合预期。

更关键的是，`comments` 表在初始 CREATE TABLE 中只有 `author_name` 列，而后续 ALTER 添加了 `author_avatar` 列放在
`author_name` 之后。但在这之后又通过 ALTER 添加了 `user_id` 和 `parent_id` 放在 `post_id` 之后。如果这些 ALTER
按照当前文件顺序执行，`author_avatar` 会被插入在 `author_name` 之后（即第3列位置），而 `user_id` 和 `parent_id` 在第1列 `id` 和第2列
`post_id` 之后。这样列顺序将是：`id, post_id, user_id, parent_id, author_name, author_avatar,
...`。这虽然功能上正确，但列顺序可能不够直观。

不过这不是严重问题，仅作为维护性提示。

- SET @firefly_comment_avatar_sql = IF((SELECT COUNT(*) FROM information_schema.COLUMNS WHERE TABLE_SCHEMA = DATABASE() AND TABLE_NAME = 'comments' AND COLUMN_NAME = 'author_avatar') = 0, 'ALTER TABLE comments ADD COLUMN author_avatar VARCHAR(512) NOT NULL DEFAULT '''' AFTER author_name', 'SELECT 1');
+ -- 考虑将 user_id 和 parent_id 的 ALTER 放在 author_avatar 之前，或者在 CREATE TABLE 中直接包含所有列，以保持列顺序更合理


─── database/schema.sql:263-265 ───
[performance · medium] **性能问题：`comments` 表的 ALTER TABLE 迁移分散为多个独立语句**

`comments` 表有12个独立的 ALTER TABLE ADD COLUMN 语句（user_id, parent_id, author_avatar, client_ip,
ip_location, client_browser, client_os, client_device, client_ua, images, is_admin, sticker_id,
personal_sticker_id），每个 ALTER TABLE 都会导致 MySQL 重建整个表。对于包含大量评论数据的生产数据库，这会显著增加迁移时间。

**建议：** 将多个 ADD COLUMN 操作合并到较少的 ALTER TABLE 语句中（每个 ALTER 最多合理合并几个列），以减少表重建次数。

- SET @firefly_comment_user_id_sql = IF((SELECT COUNT(*) FROM information_schema.COLUMNS WHERE TABLE_SCHEMA = DATABASE() AND TABLE_NAME = 'comments' AND COLUMN_NAME = 'user_id') = 0, 'ALTER TABLE comments ADD COLUMN user_id BIGINT UNSIGNED NULL AFTER post_id', 'SELECT 1');
- PREPARE firefly_comment_user_id_statement FROM @firefly_comment_user_id_sql; EXECUTE firefly_comment_user_id_statement; DEALLOCATE PREPARE firefly_comment_user_id_statement;
- SET @firefly_comment_parent_id_sql = IF((SELECT COUNT(*) FROM information_schema.COLUMNS WHERE TABLE_SCHEMA = DATABASE() AND TABLE_NAME = 'comments' AND COLUMN_NAME = 'parent_id') = 0, 'ALTER TABLE comments ADD COLUMN parent_id BIGINT UNSIGNED NULL AFTER user_id', 'SELECT 1');
+ -- 合并多个 ADD COLUMN 操作到同一个 ALTER TABLE 中，减少表重建次数
+ SET @firefly_comments_alter_sql = IF(
+   (SELECT COUNT(*) FROM information_schema.COLUMNS WHERE TABLE_SCHEMA = DATABASE() AND TABLE_NAME = 'comments' AND COLUMN_NAME = 'user_id') = 0,
+   'ALTER TABLE comments
+     ADD COLUMN user_id BIGINT UNSIGNED NULL AFTER post_id,
+     ADD COLUMN parent_id BIGINT UNSIGNED NULL AFTER user_id,
+     ADD COLUMN author_avatar VARCHAR(512) NOT NULL DEFAULT '''' AFTER author_name,
+     ADD COLUMN client_ip VARCHAR(128) NOT NULL DEFAULT ''''',
+   'SELECT 1'
+ );


─── database/schema.sql:185-192 ───
[security · medium] **安全风险：`comment_email_codes` 表缺少尝试次数上限和锁定机制**

该表虽然有 `attempts` 字段记录尝试次数，但没有内置的阈值限制或基于IP的速率限制。如果攻击者针对某个邮箱地址暴力尝试验证码，`attempts` 字段会递增但不会自动阻止后续尝试。

虽然这部分的强制限制可能在应用层实现，但数据库层面缺少防御措施（如达到最大尝试次数后自动过期或禁用验证码），增加了暴力破解的风险。

**建议：** 考虑添加 `max_attempts` 虚拟列或在应用层确保达到尝试次数上限后自动拒绝服务。

- CREATE TABLE IF NOT EXISTS comment_email_codes (
-   email VARCHAR(190) NOT NULL PRIMARY KEY,
-   code_hash CHAR(64) NOT NULL,
-   attempts TINYINT UNSIGNED NOT NULL DEFAULT 0,
-   sent_at DATETIME NOT NULL,
-   expires_at DATETIME NOT NULL,
-   INDEX idx_comment_email_codes_expiry (expires_at)
- ) ENGINE=InnoDB;
+ -- 考虑在应用层添加尝试次数限制，或在数据库中添加 CHECK 约束（MySQL 8.0.16+）：
+ -- CONSTRAINT chk_attempts CHECK (attempts <= 5)


─── database/schema.sql:185-192 ───
[bug · low] **数据完整性问题：`comment_email_codes` 表使用 `email` 作为主键，但未清理过期记录**

主键为 `email` 意味着每个邮箱地址只能有一个验证码记录。如果用户在验证码过期后再次请求，旧记录会被覆盖（使用 `INSERT ... ON DUPLICATE KEY UPDATE` 或
`REPLACE` 模式）。但当前设计中没有自动清理过期记录的机制，`idx_comment_email_codes_expiry`
索引只是用于查询加速，不会自动删除过期数据。随着时间推移，过期记录会不断累积。

不过，由于该表使用 `email` 作为主键，记录数不会超过注册用户数，不会无限增长，所以此问题影响有限。

- CREATE TABLE IF NOT EXISTS comment_email_codes (
-   email VARCHAR(190) NOT NULL PRIMARY KEY,
-   code_hash CHAR(64) NOT NULL,
-   attempts TINYINT UNSIGNED NOT NULL DEFAULT 0,
-   sent_at DATETIME NOT NULL,
-   expires_at DATETIME NOT NULL,
-   INDEX idx_comment_email_codes_expiry (expires_at)
- ) ENGINE=InnoDB;
+ -- 可选：考虑添加定时清理过期记录的事件
+ -- CREATE EVENT IF NOT EXISTS clean_expired_email_codes
+ -- ON SCHEDULE EVERY 1 HOUR
+ -- DO DELETE FROM comment_email_codes WHERE expires_at < NOW();


─── database/schema.sql:99-106 ───
[maintainability · low] **维护性问题：`dynamics` 表缺少 `updated_at` 时间戳列**

与其他所有业务表（如 `posts`、`comments`、`authors` 等）不同，`dynamics` 表没有 `updated_at TIMESTAMP NOT NULL DEFAULT
CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP` 列。如果未来需要编辑动态内容，将无法追踪最后修改时间。

**建议：** 在 `dynamics` 表中添加 `updated_at` 列，以保持与其他表的一致性。

  CREATE TABLE IF NOT EXISTS dynamics (
    id BIGINT UNSIGNED NOT NULL AUTO_INCREMENT PRIMARY KEY,
    body TEXT NOT NULL,
    images JSON NOT NULL DEFAULT (JSON_ARRAY()),
    published_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
+   updated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    INDEX idx_dynamics_date (published_at)
  ) ENGINE=InnoDB;


─── .gitignore:16-16 ───
[bug · medium] 最后一行 `mini_blog（用于后台管理系统设计时候的参考）` 看起来是一个中文注释/描述，但缺少 `#` 前缀，导致它被 Git 当作一个 `.gitignore`
匹配模式来处理。这不会产生预期的效果（因为包含中文全角括号的模式不太可能匹配任何实际文件路径），而且会误导其他开发者。应该将其改为注释或删除。

- mini_blog（用于后台管理系统设计时候的参考）
+ # mini_blog（用于后台管理系统设计时候的参考）


─── .gitignore:1-12 ───
[maintainability · low] 缺少常见的跨平台忽略模式，如 macOS 的 `.DS_Store` 和 Windows 的
`Thumbs.db`。如果项目在多平台开发环境中使用，建议添加这些模式以避免意外提交系统文件。

- node_modules/
- uploads/
- dist/
- .env
- *.log
- playwright-report/
- test-results/
- screenshots/
- *.tsbuildinfo
- .firefly-run/
- .edge-check/
- .edge-cdp*/
+ .DS_Store
+ Thumbs.db
+ *.swp
+ *.swo
+ .vscode/
+ .idea/



──────── Project Summary ────────

### 项目级总结

#### 主要问题（Top Issues）

1. **安全风险：多处硬编码或明文传播凭证**
   - `start.ps1` 硬编码 MySQL root 密码 `firefly_root_dev`，且未做任何保护（`MYSQL_PASSWORD` 环境变量明文设置）。
   - `deploy/manage.sh` 中临时文件写入密码后未及时加入清理列表，且管理员密码通过管道明文传递给 `pnpm`。
   - `database/schema.sql` 硬编码默认管理员密码哈希，标签为 `firefly-default`，外部知晓该默认密码即可直接登录。
   **影响**：凭据泄露风险极高，攻击者可获得数据库或管理员权限。

2. **数据完整性缺失：`comments` 表缺少外键约束**
   - `database/schema.sql` 中，`user_id`、`parent_id`、`sticker_id`、`personal_sticker_id` 等字段均无 FOREIGN KEY 约束，导致悬挂引用。
   **影响**：数据不一致，删除父记录后子记录成为孤儿，破坏业务逻辑。

3. **CSS 设计系统不一致与可维护性差**
   - `admin.css` 多处使用硬编码字号（如 `0.7rem`、`0.75rem`）而非设计系统变量（`--fa-text-xs`、`--fa-text-sm`）。
   - `styles.css` 中大段组件样式重复定义（如 `account-entry`、`auth-*` 等），同一个选择器 `.comment-sticker-popover-head` 被定义两次且内容不同。
   - 响应式断点分散：`admin.css` 中存在独立于全局断点系统的 768px 媒体查询，`styles.css` 中多个 `@media (max-width: 767px)` 块覆盖同一 `:root` 属性。
   **影响**：难以维护主题、响应式布局失效、样式冲突。

4. **性能问题：数据库迁移脚本无状态执行**
   - `schema.sql` 中 `INSERT IGNORE INTO post_categories` 和 `UPDATE posts SET filename = slug` 每次执行 schema 都会全表扫描，且未包裹在条件判断中，导致重复操作。
   - `comments` 表的 ALTER TABLE 迁移被拆分为 12 个独立语句，缺乏事务包裹，可能因部分失败导致不一致。
   **影响**：生产环境多次执行轻易导致性能瓶颈，且迁移不可重入。

5. **可访问性不足：文本对比度低于 WCAG AA 标准**
   - `styles.css` 中 `--muted-text`（`rgb(0 0 0 / 0.56)`）和 `--faint-text`（`rgb(0 0 0 / 0.38)`）对比度分别约为 2.8:1 和 1.8:1，远低于 AA 要求的 4.5:1（正文）和 3:1（大文本）。
   **影响**：低视力用户难以阅读，不满足合规要求。

6. **脚本健壮性不足：启动脚本缺少健康检查与环境管理**
   - `start.ps1` 启动 API 和 Web 进程后直接退出，未验证进程是否存活；`Stop-TrackedProcess` 使用路径分隔符不匹配的通配符可能误杀进程。
   - 环境变量（`MYSQL_HOST` 等）在脚本退出后残留，影响同一会话后续命令。
   - `deploy/manage.sh` 中 `ERR trap` 和 `die` 函数会重复打印错误消息，且 `load_config` 使用 `printf -v` 动态变量赋值存在注入风险。
   **影响**：开发环境不稳定，部署脚本可能意外退出或留下副作用。

7. **GPU 合成层过度使用导致性能风险**
   - `admin.css` 中三处使用 `backdrop-filter: blur()`，`styles.css` 中四个 `.wave-layer` 独立启用 `will-change: transform` 和 `translate3d`，每个都触发独立 GPU 合成层。
   - `prefers-reduced-motion` 覆盖不完整（`hero-scroll-bob`、`sakura-fall` 等动画仍会动）。
   **影响**：低端设备上页面卡顿、掉帧，电池消耗增加。

8. **跨平台兼容性遗漏**
   - `.gitignore` 缺少 `#` 注释前缀导致一条中文描述被当作匹配模式；未包含 `Thumbs.db`、`.DS_Store` 等常见系统文件。
   - `start.ps1` 重定向输出时未指定编码，中文日志可能乱码。
   **影响**：多平台开发环境下易提交垃圾文件，日志不可读。

---

#### 模块热点（Module Hotspots）

| 文件/路径                     | 评论数 | 核心问题类型                     |
|-------------------------------|--------|----------------------------------|
| `database/schema.sql`         | 11     | 安全、数据完整性、性能、迁移设计 |
| `deploy/manage.sh`            | 10     | 安全、脚本健壮性、竞态条件       |
| `apps/web/src/styles.css`     | 10     | 设计系统不一致、可访问性、性能   |
| `start.ps1`                   | 8      | 安全、健壮性、环境管理           |
| `apps/web/src/admin/admin.css`| 5      | 硬编码、响应式断点、GPU 性能     |

---

#### 跨文件关注点（Cross-Cutting Concerns）

1. **安全敏感信息处理**
   - 密码硬编码（`start.ps1`、`schema.sql`）或明文传递（`deploy/manage.sh` 管道、临时文件未及时清理）。
   - 缺乏速率限制（`schema.sql` 中 `comment_email_codes` 表无尝试次数上限）。
   - 代表路径：`start.ps1:17`、`schema.sql:45`、`deploy/manage.sh:297`。

2. **CSS 设计系统一致性**
   - 多处使用硬编码值（颜色、字号）而非 CSS 变量，且深色模式未适配。
   - 重复定义、无效声明（`content-visibility: visible`）和分散的响应式断点。
   - 代表路径：`admin.css` 全部、`styles.css` 中 `--muted-text`、`--faint-text`、`!important` 行。

3. **脚本缺乏健壮性**
   - 缺少健康检查、竞态条件（端口检查与启动间隔）、环境变量残留、错误处理重复。
   - 临时文件权限管理不当。
   - 代表路径：`start.ps1` 中 `Test-PortAvailable`、`Stop-TrackedProcess`；`deploy/manage.sh` 中 `acquire_lock`、`die` 与 `ERR trap`。

4. **数据库迁移设计缺陷**
   - 外键约束缺失、无状态执行（重复 INSERT/UPDATE）、迁移语句分散且无事务。
   - 代表路径：`schema.sql` 中 `comments` 表所有 ALTER TABLE、`INSERT IGNORE INTO post_categories`、`UPDATE posts SET filename`。