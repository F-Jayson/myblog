# 审查核对与修复报告

> 核对日期：2026-08-23  
> 原始依据：根目录《审查报告.md》  
> 核对状态：45/45 项已完成逐条核对

## 一、审查范围与处理原则

本次工作逐条核对《审查报告.md》中的 45 项具体意见，范围覆盖前台与后台样式、Windows 启停脚本、Linux 部署脚本、Docker Compose、数据库结构与种子数据、管理员初始化 CLI、忽略规则及配套文档。

处理遵循以下原则：

1. 以当前仓库代码、实际控制流和可复核证据为准，不因原报告标注了高等级就直接采纳。
2. 能证明存在行为错误、安全缺陷、兼容性问题或明确维护成本的意见予以采纳并修复。
3. 一条意见中只有部分事实成立时标记为“部分采纳”，只修改成立的部分。
4. 仅有推测、缺少测量、误读现有实现或建议本身会扩大风险的意见予以驳回。
5. 修复保持现有项目结构和参考站视觉，不修改独立参考项目 `Firefly-master/`，也不修改原《审查报告.md》。
6. 数据库变更以可重复执行和兼容旧库为目标；没有真实 MySQL 8.4 验证的内容不标记为“实库验证通过”。

## 二、总体进度

| 项目 | 数量 | 进度 |
|---|---:|---|
| 已核对审查项 | 45 | 45/45，100% |
| 采纳并完成修复 | 27 | 27/27 |
| 部分采纳并完成成立部分 | 3 | 3/3 |
| 驳回，无需按原建议修改 | 15 | 15/15 |
| MySQL 8.0.45 受控实库验证 | 4 组场景 | 新库、二次幂等、冲突中止、受限权限均通过 |
| MySQL 8.4 实库迁移验证 | 0 | 当前环境受限，待验证 |

说明：“已修复”表示代码或文档修改已经落地。`database/schema.sql` 已在 MySQL 8.0.45 完成受控新库、二次幂等、冲突保护和受限权限验证；MySQL 8.4、完整历史库快照、孤儿数据清理及大表锁影响仍待补充验证。

## 三、改动文件摘要

| 文件 | 修改摘要 |
|---|---|
| `.env.docker.example` | 移除 Docker MySQL 示例默认密码，要求部署者显式设置唯一高强度密码。 |
| `.gitignore` | 补充 macOS、Windows、编辑器与 IDE 常见忽略项；保留真实参考目录的中文精确匹配规则。 |
| `apps/web/src/admin/admin.css` | 统一真实存在的硬编码字号，合并 `.fa-settings-quick` 重复规则。 |
| `apps/web/src/styles.css` | 增加 sRGB/OKLCH 渐进增强，清理重复社区样式和无效声明，修正导航搜索主题色、移动字号冲突、弱文本对比度及 reduced-motion 覆盖。 |
| `start.ps1`、`start.bat` | 增加 `.env` 解析、动态 API 端口、端口回退、Docker 状态检查、HTTP 健康检查、分阶段进程状态、失败清理和环境变量恢复；隐藏 API/Web 服务控制台，并在批处理入口成功后保留窗口显示访问地址，按键关闭提示不停止服务。 |
| `stop.ps1` | 严格校验 PID、命令行和创建时间；损坏或不可信状态文件保留诊断信息，仅在受管进程均确认停止后删除状态。 |
| `deploy/manage.sh` | 加固锁目录和敏感临时文件清理，补充系统信息解析错误，调整数据库迁移与管理员初始化顺序，移除默认管理员凭据依赖。 |
| `docker-compose.yml` | 使用专用应用数据库用户、必填密码、环境化端口和随机 root 密码；健康检查改为验证应用凭据，且不再首次启动时自动执行 seed。 |
| `apps/api/src/cli.ts` | 移除公开默认管理员哈希逻辑；`set-admin-password` 支持终端隐藏输入与二次确认，并在事务中 UPSERT `admin`、撤销旧会话。 |
| `database/schema.sql` | 补齐新库列定义、旧库幂等迁移、评论关系外键、按需数据回填，并移除默认管理员插入。 |
| `database/seed.sql` | 移除默认管理员凭据，seed 仅负责演示内容。 |
| `README.md` | 删除默认账号密码说明，改为显式执行管理员密码 CLI，并说明 Docker 密码、按需 seed、容器停止方式及 `start.bat` 成功后的地址提示行为。 |
| `deploy/DEPLOYMENT.md` | 明确 migrate/seed 不创建管理员，并说明首次部署、更新和重新配置的凭据流程。 |

## 四、逐项核对与修复状态

| 编号 | 原问题简述 | 结论 / 状态 | 修复或驳回依据与当前证据 |
|---:|---|---|---|
| 1 | `.fa-empty`、`.fa-placeholder p` 使用硬编码字号 | 采纳 / 已修复 | `apps/web/src/admin/admin.css` 已分别使用 `var(--fa-text-xs)`、`var(--fa-text-sm)`，与后台设计令牌一致。 |
| 2 | `.fa-settings-quick` 分散重复定义 | 采纳 / 已修复 | 重复规则会造成维护歧义；当前已合并为一处规范规则，按钮与链接样式集中维护。 |
| 3 | 三处 `backdrop-filter` 必然造成严重 GPU 性能问题 | 驳回 / 无需修改 | 报告只给出泛化推测，没有设备、帧率、绘制时间或内存测量；模糊层承担明确视觉功能，不能仅凭属性出现判定性能缺陷。 |
| 4 | 多种后台表格在 768–1024px 必然布局断裂 | 驳回 / 无需修改 | 表格由 `.fa-table`、审计页响应式规则和可横向滚动容器共同处理；存在 `min-width` 不等于页面不可用，报告未给出可复现溢出证据。 |
| 5 | 审计页独立 768px 媒体块违反断点体系 | 驳回 / 无需修改 | 768px 本身属于现有断点，局部规则按功能就近组织是合理维护方式；“媒体块分散”本身不是行为错误。 |
| 6 | 全局 OKLCH 变量没有旧浏览器降级 | 采纳 / 已修复 | `apps/web/src/styles.css` 先声明 sRGB 默认值，再在 `@supports (color: oklch(...))` 中覆盖为 OKLCH，不支持 OKLCH 的浏览器仍有可读颜色。 |
| 7 | `.nav-search` 使用硬编码颜色和 `!important` | 采纳 / 已修复 | 当前规则改用主题变量/混色，并移除不必要的 `!important`，浅色和暗色主题可正常覆盖。 |
| 8 | 社区账户与评论组件存在大段重复 CSS | 采纳 / 已修复 | 原两套同名选择器数值不同，后者会覆盖前者；当前仅保留并整合一套 “Public account and discussion surfaces” 规则。 |
| 9 | 四层 wave 的合成属性必然造成移动端性能问题 | 驳回 / 无需按原建议删层 | 未提供真实性能测量，合成层也不能单独证明卡顿；直接删层会偏离参考站视觉。运动偏好问题另在第 10 项处理。 |
| 10 | `prefers-reduced-motion` 覆盖不完整 | 采纳 / 已修复 | `styles.css` 已为全局动画补充 `animation-iteration-count: 1`，并对波浪、滚动提示、樱花、加载、图片过渡等关键动态效果增加禁用或极短时长覆盖。 |
| 11 | 多个 767px 媒体块以及 15px/14px 根字号冲突 | 部分采纳 / 已修复成立部分 | 真正冲突的是同一移动视口下两次设置根字号，当前只保留 14px；媒体块按组件分散不必强制合成一个超大规则块。 |
| 12 | `content-visibility: visible` 是无效冗余声明 | 采纳 / 已修复 | `visible` 为默认行为，在现有用法中无性能收益；相关冗余声明已删除。 |
| 13 | `--muted-text` 与 `--faint-text` 均不满足 AA | 部分采纳 / 已修复成立部分 | `--faint-text` 的确过浅，浅色/暗色值均已提高；`--muted-text` 在实际浅色背景复算约 4.95:1，报告所称约 3:1 不成立，故未盲目加深。 |
| 14 | `.comment-sticker-popover-head` 被冲突定义两次 | 驳回 / 无需按原建议修改 | 原代码是主选择器和其 `button` 子选择器，并非两个相同选择器互相覆盖；当前随第 8 项整合后仍各保留一处，结构明确。 |
| 15 | `stop.ps1` 解析失败仍删除状态文件 | 采纳 / 已修复 | 当前 `ConvertFrom-Json` 失败会告警并保留 `processes.json`；只有两个进程均确认停止且状态文件删除成功后才报告成功。 |
| 16 | `start.ps1` 启动进程后没有健康检查 | 采纳 / 已修复 | `Wait-ForServices` 同时检查 API/Web 进程与 HTTP 响应；数据库模式要求 `/api/health` 返回 200，失败会停止已启动进程并保留必要诊断。 |
| 17 | Docker 模式硬编码 root 用户和密码 | 采纳 / 已修复 | `Initialize-DockerEnvironment` 从 `.env` 读取并校验专用非 root 用户；Compose 使用同一 `MYSQL_USER`/`MYSQL_PASSWORD`，root 密码改为随机生成。 |
| 18 | `Start-Process` 重定向未设置编码 | 驳回 / 无可靠原建议可实施 | `Start-Process -RedirectStandardOutput/-Error` 没有编码参数；设置 `$PSDefaultParameterValues` 或控制台编码不能可靠改变 Node/pnpm 写入管道的字节编码，报告建议不能解决所述问题。 |
| 19 | Docker MySQL 健康等待无失败与进度反馈 | 采纳 / 已修复 | 当前检查容器 ID、运行/退出状态与健康状态，定期输出等待信息，失败时显示最近日志并明确终止。 |
| 20 | `start.ps1` 永久改变调用者工作目录 | 采纳 / 已修复 | 已移除全局 `Set-Location`；`Invoke-Pnpm` 使用 `Push-Location`/`Pop-Location` 并由 `finally` 保证恢复。 |
| 21 | Docker 数据库环境变量在会话中残留 | 采纳 / 已修复 | `Save-ProcessEnvironment` 保存受管键原值，脚本外层 `finally` 逐项恢复，覆盖正常和异常退出路径。 |
| 22 | 停止进程使用通配符误判且 CIM 失败静默 | 采纳 / 已修复 | 当前使用字面量、大小写不敏感比较，并记录完整命令行与 `CreationDate`；CIM、`taskkill` 或退出确认失败均显式告警并保留 state。 |
| 23 | 端口检测无兼容回退，检查与启动存在竞态 | 采纳 / 已修复 | `Test-PortAvailable` 在 `Get-NetTCPConnection` 不可用/失败时使用 `.NET TcpListener` 绑定测试；启动后的进程与 HTTP 健康检查覆盖竞态结果。 |
| 24 | MySQL 凭据临时文件登记清理过晚 | 采纳 / 已修复 | `make_mysql_defaults_file` 在 `mktemp` 后立即把路径加入 `SECRET_TEMP_FILES`，之后才设权限和写入凭据。 |
| 25 | 管理员密码经 stdin 管道传递属于高危泄漏 | 驳回 / 保留 stdin 方案 | stdin 内容不出现在 argv；能读取目标进程内存或 fd 的同权限/root 主体本就越过该保密边界。改用临时文件会增加密码落盘和异常残留风险，当前 `printf '%s'` 也避免附加换行。 |
| 26 | 固定锁文件可能遭符号链接攻击 | 采纳 / 已修复 | 锁已移至受保护的 `/var/lock/firefly-react` 目录，并校验目录非符号链接、root 所有权与权限后再使用 `flock`。 |
| 27 | `ERR` trap 与 `die` 一定重复输出 | 驳回 / 无需修改 | Bash 中显式 `exit 1` 不按报告假设必然触发 ERR trap，未发现可复现的重复输出；保留 trap 有助定位未被 `die` 包装的失败。 |
| 28 | 白名单后的 `printf -v` 仍可注入任意变量 | 驳回 / 无需修改 | 配置键先通过标识符检查，再经过固定 `case` 白名单；`PATH`、`LD_PRELOAD` 或任意键无法到达 `printf -v`。 |
| 29 | `configure` 重装与构建必然是不必要性能问题 | 驳回 / 保持一致性流程 | `configure` 的职责是把服务恢复到完整可运行状态，依赖安装为幂等操作，构建还能验证配置与产物；报告没有提供停机或耗时测量。 |
| 30 | `/etc/os-release` 解析失败会静默退出 | 采纳 / 已修复 | `require_supported_os` 现在对 `source /etc/os-release` 失败给出明确 `die` 信息。 |
| 31 | schema/seed 内置公开默认管理员哈希 | 采纳 / 已修复；8.0.45 已验证 | `schema.sql` 与 `seed.sql` 已删除默认管理员；CLI 使用随机盐，在事务中 UPSERT `admin` 并撤销旧会话。MySQL 8.0.45 新库验证确认迁移不再写入默认管理员；CLI 类型检查及隐藏输入运行时检查通过。 |
| 32 | `comments.user_id` 缺少外键 | 采纳 / 已修复；8.0.45 已验证 | schema 按引用表、引用列及 `SET NULL` 语义检测外键，缺失时先清理孤儿再添加约束。MySQL 8.0.45 新库、二次迁移及冲突中止场景通过；完整历史孤儿数据矩阵待验证。 |
| 33 | `comments.parent_id` 缺少自引用外键 | 采纳 / 已修复；8.0.45 已验证 | 已加入语义检测、孤儿父 ID 清理和 `ON DELETE SET NULL` 自引用约束；MySQL 8.0.45 新库和二次迁移通过，完整历史孤儿数据矩阵待验证。 |
| 34 | `authors.profile` 只在迁移、不在新库定义 | 采纳 / 已修复；8.0.45 已验证 | 新库定义直接包含 `profile JSON`，动态 SQL 继续兼容旧库缺列升级；MySQL 8.0.45 新库和二次迁移通过，真实旧库快照待验证。 |
| 35 | `post_categories` 每次无条件重复回填 | 采纳 / 已修复；8.0.45 已验证 | 当前先检查是否存在缺失映射，仅在需要时执行带 `NOT EXISTS` 的 `INSERT IGNORE`；MySQL 8.0.45 新库和二次迁移通过，大表锁与历史部分映射场景待验证。 |
| 36 | `comments.sticker_id` 缺少外键 | 采纳 / 已修复；8.0.45 已验证 | 已按 `comment_stickers(id)` 与 `SET NULL` 语义检测，缺失时清理孤儿后添加约束；MySQL 8.0.45 新库和二次迁移通过，完整历史孤儿数据矩阵待验证。 |
| 37 | `comments.personal_sticker_id` 缺少外键 | 采纳 / 已修复；8.0.45 已验证 | 已按 `comment_user_stickers(id)` 与 `SET NULL` 语义检测，缺失时清理孤儿后添加约束；MySQL 8.0.45 新库和二次迁移通过，完整历史孤儿数据矩阵待验证。 |
| 38 | filename 条件 UPDATE 会“大量无意义更新” | 部分采纳 / 已修复成立部分；8.0.45 已验证 | 原语句已有异常值 `WHERE`，不会更新正常行，因此“大量无意义更新”定性不成立；但每次迁移仍会执行条件 UPDATE。当前以 `EXISTS` 配合按需 `PREPARE`，无异常行时不执行 UPDATE；MySQL 8.0.45 新库和二次迁移通过，大表扫描与锁影响待验证。 |
| 39 | `author_avatar AFTER author_name` 列顺序是 bug | 驳回 / 无需修改 | `AFTER author_name` 结果确定且不影响查询或数据完整性；报告正文也承认功能正确，仅属主观列顺序偏好。 |
| 40 | comments 多个独立 ALTER 应无条件合并 | 驳回 / 保持逐列幂等迁移 | 旧库可能处于任意部分迁移状态，逐列存在性检查可安全续跑；报告示例只检查首列，反而会在部分升级库中重复加列或漏列。 |
| 41 | 验证码表没有数据库 CHECK 即可无限尝试 | 驳回 / 应用层已有控制 | 验证逻辑在应用层按邮箱检查有效期与尝试次数，达到 5 次后拒绝；数据库 CHECK 只会让计数 UPDATE 报错，不能代替正确锁定和限流。 |
| 42 | 过期验证码必然无限增长 | 驳回 / 无需数据库 EVENT | `email` 是主键，新验证码覆盖同邮箱旧记录，验证成功后删除；记录上限按不同邮箱数增长，不会按请求无限累积，数据库 EVENT 还会增加部署权限依赖。 |
| 43 | `dynamics` 缺少 `updated_at` | 采纳 / 已修复；8.0.45 已验证 | 新库定义已包含自动更新时间，旧库另有存在性检查和动态 `ALTER TABLE`；MySQL 8.0.45 新库和二次迁移通过，真实旧库缺列升级待验证。 |
| 44 | `.gitignore` 中文行误写成未注释说明 | 驳回 / 保留规则 | 工作区实际存在名为 `mini_blog（用于后台管理系统设计时候的参考）` 的参考目录，该行是有效精确忽略模式，不是注释。 |
| 45 | 缺少常见跨平台忽略项 | 采纳 / 已修复 | 已加入 `.DS_Store`、`Thumbs.db`、`*.swp`、`*.swo`、`.vscode/`、`.idea/` 等规则。 |

## 五、验证结果

以下检查均基于最终修改后的工作区执行：

| 检查 | 结果 | 说明 |
|---|---|---|
| `pnpm type-check` | 通过 | API 与 Web 的 TypeScript 检查均无错误。 |
| `pnpm build` | 通过 | API 与 Web 生产构建成功；保留既有的 `MarkdownContent.tsx` 静态/动态混合导入及约 1.6 MB 主 chunk 警告，不影响本次构建通过。 |
| PowerShell 7 与 Windows PowerShell 5.1 AST 解析 | 通过 | `start.ps1`、`stop.ps1` 在两代 PowerShell 均无语法错误。 |
| `bash -n deploy/manage.sh` | 通过 | Linux 部署脚本语法检查无错误。 |
| `bash deploy/manage.sh help` | 通过 | 帮助路径可正常执行并列出全部管理命令。 |
| `docker compose config --quiet` | 通过 | 使用临时非空强密码验证；空密码会按预期被 Compose 拒绝。 |
| Compose `$` 特殊字符隔离检查 | 通过 | 插值环境中的密码与输入逐字一致；`config` 模型中的 `$$` 是 Compose 的转义表示。 |
| 管理员 CLI 交互检查 | 通过 | 终端两次输入均不回显，显示确认提示；短密码在建立数据库连接前被长度校验拒绝。PowerShell 与 Bash 管道输入也能正确进入长度校验。 |
| CSS 解析与生产打包 | 通过 | PostCSS/构建可解析两份 CSS；评论样式去重后的三个级联遗漏已恢复。 |
| `git diff --check` | 通过 | 无空白错误；仅输出 Git 的 LF/CRLF 工作区转换提示。 |

### MySQL 8.0.45 实测

使用 MySQL 8.0.45 和项目实际依赖 `mysql2` 3.23.3，按 CLI 的单次 `multipleStatements` 调用方式执行了 `database/schema.sql`：

- 干净库首次迁移成功，约 2.619 秒；生成 23 张表、199 列、51 个约束和 15 个外键。
- 同库第二次迁移成功，约 92 毫秒；两次结构指纹均为 `51a7bb617eb14d85ad20ef9e001de138e06626ab268acf70c85443a54b802bae`。
- 构造 `comments.user_id` 同名冲突外键后，迁移按预期以 `ER_BAD_FIELD_ERROR`（1054 / SQLSTATE 42S22）中止；哨兵孤儿值未清理、错误外键仍保留、正确外键未新增，后续语句也未执行。
- 仅授予 `SELECT, INSERT, UPDATE, DELETE, CREATE, REFERENCES, INDEX, ALTER` 的账户可完成首次和二次迁移，不需要 routine 权限，也未留下辅助数据库对象。
- 最终 `schema.sql` 的 SHA-256 为 `447debd6a025b53a1627912a719651d78e8370cb12080b459de4ee5d24133889`，大小 37,912 字节。

### 当前环境限制

当前 Windows 环境的 Docker Linux daemon 不可用，因此无法启动 Compose 中的 MySQL 8.4 容器。MySQL 8.0.45 的上述结果不能替代以下尚未执行的验证：

- MySQL 8.4 上的干净库、二次迁移与冲突保护复测；
- 真实历史旧库快照中缺列、缺外键、部分迁移状态及孤儿数据的完整升级；
- `posts`、`post_categories`、`comments` 大数据量下的执行时间、扫描和锁影响。

## 六、MySQL 8.4 后续验证建议

### 1. 新库验证

1. 在空 MySQL 8.4 实例创建专用非 root 应用用户，并授予目标库所需权限。
2. 执行 `pnpm db:migrate`，确认所有表、索引、JSON 默认值、`authors.profile`、`dynamics.updated_at` 及四个评论关系外键均创建成功。
3. 可选执行 `pnpm db:seed`，确认 seed 只写演示数据且不会创建或修改管理员。
4. 使用 `printf '%s' '<unique-password>' | pnpm --filter @firefly-rebuild/api exec tsx src/cli.ts set-admin-password` 初始化管理员，验证 `admin` 可登录且数据库中不存在公开默认哈希。

### 2. 旧库升级验证

1. 从修复前结构建立脱敏快照，覆盖缺少 `profile`、`updated_at`、评论关系外键及部分列已存在等历史状态。
2. 人为准备合法关系、孤儿关系及已有 `post_categories` 部分映射，执行一次迁移。
3. 确认合法关系保留、孤儿评论关系被置为 `NULL`、缺失文章分类映射被补齐、空 filename 回填为 slug。
4. 检查 `information_schema.KEY_COLUMN_USAGE` 与 `REFERENTIAL_CONSTRAINTS`，确认引用表/列和 `DELETE_RULE` 均符合预期。
5. 若旧库已有与目标名称相同但语义不同的外键，先记录并人工修正，避免自动删除未知约束。

### 3. 二次幂等迁移验证

1. 在完成新库或旧库迁移后立即再次执行 `pnpm db:migrate`。
2. 确认第二次执行无 SQL 错误、无重复列/索引/约束，也不改变业务行数。
3. 对迁移前后关键表行数、外键清单和管理员哈希做快照比对；第二次执行后结果应保持一致。
4. 记录第二次执行耗时和锁等待，重点观察 `posts`、`post_categories`、`comments` 大表查询。
5. 将结果与本报告的 MySQL 8.0.45 基线比较；只有 8.4 与历史库矩阵完成后，才将相应项目标记为“完整矩阵已验证”。

## 七、结论

45 条意见已全部完成事实核对：27 条成立并已修复，3 条部分成立且已修复成立部分，15 条因证据不足、事实不符或建议会扩大风险而驳回。通用回归与 MySQL 8.0.45 受控迁移验证均已通过；尚未覆盖的 MySQL 8.4、完整历史库快照和大表锁影响已作为明确验证边界保留。

## 八、本轮启动窗口补充修复

| 项目 | 状态 | 结果 |
|---|---|---|
| 启动入口 | 已修复 | Windows 双击入口统一为 `start.bat`，README 已同步，避免继续指向已移除的 `start.cmd`。 |
| 空白服务窗口 | 已修复 | API 与 Web 的 `Start-Process` 使用 `-WindowStyle Hidden`；服务日志仍写入 `logs/`。 |
| 成功提示窗口 | 已修复 | `start.ps1` 输出 Web、Admin、API、Health、日志和停止脚本地址；`start.bat` 成功后执行 `pause`，窗口不会立即关闭。 |
| 关闭提示后的服务 | 已验证 | 按键只结束批处理提示窗口，受管 API/Web 进程继续运行；当前运行态 HTTP 检查与隐藏窗口检查均通过。 |

验证边界：经典旧版 `cmd.exe` 是否提供鼠标直接点击 URL 取决于宿主终端；Windows Terminal 通常可使用 Ctrl+点击，地址文本始终会保留在窗口中。
